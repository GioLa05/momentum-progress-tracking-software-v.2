// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "momentum.redberryinternship.ge",
      "api.dicebear.com",
    ],
  },
};

module.exports = nextConfig;
